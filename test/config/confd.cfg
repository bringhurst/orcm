config
!
orcm
 install
  app client-server
   exec server
    default
     replicas 2
     argv     [ -t 65530 ]
     max-restarts 1
    !
   !
   exec client
    default
     replicas 1
     max-restarts -1
    !
   !
   !exec client2
   ! default
   !  replicas 1
    ! 
   !
 !
  app talker-listener
   exec talker
    default
     replicas     2
     argv         [ -t p0 ]
     max-restarts 10
    !   
   !
   exec listener
    default
     replicas     1
     max-restarts 10
    !
   !
  !
  app talker-iovec
   exec talker_iovec
   default
    replicas 1
    max-restarts -1
   !
  !
 !
!
run
 !
 app-instance cstest app-name client-server
 !
 app-instance tltest app-name talker-listener
 !
 app-instance titest app-name talker-iovec
 !
!

commit
end
